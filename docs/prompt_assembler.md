# PromptAssembler Specification

## Overview
`PromptAssembler` compiles the minimal valid prompt context for model requests.

## Dynamic Component Injection
- Profile parameters
- Dynamic memory injection
- Dynamic RAG context
- Dynamic food injection (intent-based)
- Session context (window, app, clipboard)
- Compressed conversation history
- Token Budget enforcement (`TokenBudgetManager`)
